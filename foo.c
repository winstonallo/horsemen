#include <unistd.h>
#include <fcntl.h>
#include <stdio.h>

int main(/*int ac, char**av*/) {
    // (void)ac;


    // int fd = open(av[1], O_WRONLY | O_APPEND, 0644);
    // if (fd < 0) {
    //     printf("fuck u\n");
    //     return 1;
    // }

    // char buf[0x3000] = {0};

    // if (write(fd, buf, sizeof(buf)) < 0) {
    //     printf("u too");
    //     return 1;
    // }


    // close(fd);

    return 123;
}
